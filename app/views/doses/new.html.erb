<h1> Add a dose </h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, collection: @ingredients %>
  <%= f.association :cocktail, as: :hidden, value: @cocktail.id %>
  <%= f.submit %>
  <% end %>
  </div>
</div>
